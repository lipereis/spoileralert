# Task 8 implementation brief — Documentation and Final Verification

Complete Task 8 from the approved plan. Update README.md to document complete current-calendar-year scope for every submitted public profile; observed/calculated/inferred distinctions; all ten archetypes and deterministic tie priority; denominators and missing-weight renormalization; mood mappings; director/timeline rules; six ordered filenames and ZIP behavior; honest no-metadata fallback; and Streamlit Community Cloud secret setup with `TMDB_API_KEY` (never a real key).

Audit the complete project against global constraints and fix only verified defects. Search for secrets, stale recent-diary wording, unsafe usernames/HTML, raw external errors, random card behavior, temporary card files, and obsolete APIs.

Run fresh full unittest discovery, compileall, import smoke, deterministic six-card/ZIP verification, and Streamlit hidden health check on an available local port, stopping only the exact recorded process. Run real Streamlit AppTests for valid enhanced fixture, invalid user/error, no-key/metadata failures, gallery/download metadata, and Create Another as available. Do not claim external real-profile/TMDB verification without credentials/network.

If browser tooling is available, inspect landing/result at desktop/tablet/mobile and focus/reduced motion; otherwise record the limitation honestly. Use verification-before-completion discipline. Write `task-8-report.md` with exact commands, outputs/counts, external limitations, files changed, and checklist. No Git commit.
